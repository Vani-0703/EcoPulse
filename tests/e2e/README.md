# E2E test plan

The production E2E suite should exercise: login → dashboard → CSV upload → analytics refresh → anomaly/forecast → recommendation. The local Docker stack provides the dependencies needed for browser automation.
