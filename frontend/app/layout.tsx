import './globals.css'; import type {Metadata} from 'next'; export const metadata:Metadata={title:'EcoPulse | Sustainability Intelligence',description:'Resource analytics, anomaly detection and sustainability intelligence.'}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
