'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {api} from '../lib/api';
export default function Home(){const [email,setEmail]=useState('demo@ecopulse.local'),[password,setPassword]=useState('password'),[error,setError]=useState(''),[loading,setLoading]=useState(false);const router=useRouter();async function login(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');try{const x=await api<{token:string}>('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('ecopulse_token',x.token);router.push('/dashboard')}catch(e){setError(e instanceof Error?e.message:'Unable to sign in')}finally{setLoading(false)}}return <main className="min-h-screen grid lg:grid-cols-2"><section className="p-10 lg:p-20 flex flex-col justify-center"><div className="max-w-xl"><div className="text-sm tracking-[.3em] uppercase text-emerald-300">ECOPULSE</div><h1 className="text-5xl lg:text-7xl font-bold mt-5 leading-tight">Turn resource data into <span className="text-emerald-300">better decisions.</span></h1><p className="muted text-lg mt-6">A sustainability intelligence platform for monitoring consumption, detecting anomalies, forecasting demand and prioritizing practical actions.</p><div className="grid grid-cols-3 gap-3 mt-10"><div className="card p-4"><b>Energy</b><div className="muted text-sm mt-1">Track & compare</div></div><div className="card p-4"><b>ML</b><div className="muted text-sm mt-1">Detect & forecast</div></div><div className="card p-4"><b>Action</b><div className="muted text-sm mt-1">Estimate savings</div></div></div></div></section><section className="p-8 flex items-center justify-center bg-black/10"><form onSubmit={login} className="card p-8 w-full max-w-md"><h2 className="text-2xl font-bold">Sign in</h2><p className="muted mt-2">Use the seeded demo account to explore the system.</p><label className="block mt-7 text-sm">Email<input className="input mt-2" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block mt-4 text-sm">Password<input className="input mt-2" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="mt-4 p-3 rounded-lg bg-red-950/40 text-red-200">{error}</div>}<button className="btn btn-primary w-full mt-6" disabled={loading}>{loading?'Signing in…':'Enter EcoPulse'}</button><p className="muted text-xs mt-4">Demo credentials: demo@ecopulse.local / password</p></form></section></main>}
